# ========== SERVER CONFIGURATION ==========
# Node.js environment setting. Use 'production' for live apps and 'development' for local testing
NODE_ENV=development
# Define which port the server should run on. Default is usually 3000 or 5000
PORT=3000

# ========== JWT CONFIGURATION ==========
# Secret used to sign JWT tokens. Keep this secret safe, especially in production
JWT_SECRET=your_jwt_secret
# Secret for refreshing JWT tokens when they expire. Also keep this secret safe
JWT_REFRESH_SECRET=your_jwt_refresh_secret

# ========== STRIPE CONFIGURATION ==========
# Stripe secret API key for handling payments. Get this from the Stripe dashboard
STRIPE_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXX
# Secret key used to validate Stripe webhook events. Found in the Stripe dashboard (Webhook section)
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXX

# ========== GOOGLE DRIVE CONFIGURATION ==========
# OAuth Client ID and Secret for integrating Google Drive. These can be obtained from the Google Developer Console
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
# The URL to which Google should redirect after successful OAuth authentication
GOOGLE_REDIRECT_URI=http://localhost:3000/api/googledrive/callback

# ========== DROPBOX CONFIGURATION ==========
# OAuth Client ID and Secret for integrating Dropbox. Get these from the Dropbox Developer Console
DROPBOX_CLIENT_ID=your_dropbox_client_id
DROPBOX_CLIENT_SECRET=your_dropbox_client_secret
# Dropbox redirect URI for OAuth
DROPBOX_REDIRECT_URI=http://localhost:3000/api/dropbox/callback

# ========== DATABASE CONFIGURATION ==========
# Database connection URL in the format: postgres://username:password@host:port/database
DATABASE_URL=postgres://user:password@localhost:5432/yourdb

# ========== EMAIL CONFIGURATION ==========
# SMTP credentials for sending emails (e.g., notifications, receipts). Use your email provider’s SMTP server details
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-email-password

# ========== FRONTEND CLIENT CONFIGURATION ==========
# Frontend URL used for redirects and other communication from the backend (i.e., when payments are completed)
CLIENT_URL=http://localhost:3000

# ========== FOLDERPAL REVENUE SHARING CONFIGURATION ==========
# Define the percentage of the revenue share for FolderPal (if applicable)
FOLDERPAL_SHARE=0.20

# Optional variables for additional third-party services can be added below